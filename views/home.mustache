<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="../public/todo.css">
    <title>To Do List</title>
  </head>
  <body>
    <form class="container" action = "/" method = post>
      <input type=text name="tasks" placeholder="Add a todo...">
      <button type="submit" name="submit">Add todo</button>
    </form>
    <ul class="tasks">
      {{#tasks}}
      {{#yetTodo}}
        <li>
          <form action="/complete" method="post">
            <button type="submit" name="list" value="todo">Mark completed</button>
          </form>
        </li>
      {{/yetTodo}}
      {{/tasks}}
    </ul>
    <h2>Completed tasks</h2>
    <ul>
      {{#todos}}
      {{^yetTodo}}
      <li>
        {{todo}}
      </li>
      {{/yetTodo}}
      {{/todos}}
    </ul>

  </body>
</html>
